/**
 * New name added to states, this is to avoid confusion with the NGXS library
 */

export interface Statal {
  id?: string;
  name?: string;
  polygon?: string;
  createdAt?: string;
  updatedAt?: string;
}

export interface Municipality {
  id?: string;
  name?: string;
  polygon?: string;
  createdAt?: string;
  updatedAt?: string;
}
